diff --git a/dispsolver/solver/dynamic.py b/dispsolver/solver/dynamic.pyindex b432bf3..472508c 100644--- a/dispsolver/solver/dynamic.py+++ b/dispsolver/solver/dynamic.py@@ -120,27 +120,37 @@ def _equilibrate(A: sps.csr_matrix, b: np.ndarray):      For KKT saddle-point systems the mechanical block (K_eff ??10?? and the     constraint block (C ??1) differ by 5 orders of magnitude.  Equilibration-    rescales every row/column so |횄_ii| ??1, giving PARDISO a uniformly-scaled-    system whose pivot search is no longer biased by this unit mismatch.+    rescales every row/column so the largest entry in each row/column is ??1,+    giving PARDISO a uniformly-scaled system. -    Lagrange-multiplier rows with |J_ii| < 1e-30 are assigned the median scale-    of the mechanical rows, balancing the C-block entries without making the-    zero-diagonal rows vanish in the scaled system.+    For zero-diagonal rows (Lagrange multipliers) the scale is derived from+    the max absolute off-diagonal entry in that row, so constraint Jacobian+    entries do not vanish in the scaled system.     """     diag = A.diagonal().copy()     abs_diag = np.abs(diag)-    # Median of non?몒ero mechanical diagonals+    n = A.shape[0]+    scale = np.zeros(n, dtype=np.float64)+    +    # Rows with non-zero diagonal: scale by sqrt(diag)     mech_mask = abs_diag > 1e-30     if np.any(mech_mask):-        med = float(np.median(abs_diag[mech_mask]))-        med_sqrt = np.sqrt(med)-    else:-        med_sqrt = 1.0-    scale = np.where(mech_mask, 1.0 / np.sqrt(abs_diag), 1.0 / med_sqrt)-+        scale[mech_mask] = 1.0 / np.sqrt(abs_diag[mech_mask])+    +    # Zero-diagonal rows (LM/constraint rows): scale by sqrt(max_offdiag)+    zero_mask = ~mech_mask+    if np.any(zero_mask):+        for row in np.where(zero_mask)[0]:+            s, e = A.indptr[row], A.indptr[row + 1]+            if e > s:+                max_val = np.max(np.abs(A.data[s:e]))+                scale[row] = 1.0 / np.sqrt(max(max_val, 1e-30))+            else:+                scale[row] = 1.0+         # Symmetric scaling:  횄_ij = A_ij 쨌 s_i 쨌 s_j     As = A.copy()-    for row in range(A.shape[0]):+    for row in range(n):         s, e = As.indptr[row], As.indptr[row + 1]         As.data[s:e] *= scale[row]         As.data[s:e] *= scale[As.indices[s:e]]@@ -465,12 +475,15 @@ class DynamicSolver:         material_params: Optional[Dict] = None,         constraints: Optional[List[BaseConstraint]] = None,         penalty_constraints: Optional[List] = None,+        rbe2_elements: Optional[List] = None,         beta: float = 0.25,         gamma: float = 0.5,         max_iter: int = 20,         tol: float = 1e-8,         atol: float = 1e-9,         rtol: float = 1e-4,+        force_atol: float = 1e-3,+        max_du_atol: float = 1e-3,         verbose: bool = False,         max_step: Optional[float] = None,         element_type: str = "Q4",@@ -479,6 +492,7 @@ class DynamicSolver:         static_mode: bool = False,         alpha: float = 0.0,         mode: Optional[str] = None,+        viscous_stab_factor: float = 0.0,     ):         # Named integration preset overrides the individual 棺/款/慣/static_mode         # arguments with a mutually-consistent set (see INTEGRATION_MODES).@@ -516,6 +530,9 @@ class DynamicSolver:         self.tol = tol         self.atol = atol   # absolute displacement-correction floor (P2)         self.rtol = rtol   # relative residual-force convergence tolerance (P2)+        self.force_atol = force_atol+        self.max_du_atol = max_du_atol+        self.viscous_stab_factor = viscous_stab_factor  # Abaqus-style stabilization         self.verbose = verbose         self.max_step = max_step         self.static_mode = static_mode@@ -533,10 +550,12 @@ class DynamicSolver:             self.element_type = element_type         self.constraints = constraints if constraints is not None else []         self.penalty_constraints = penalty_constraints if penalty_constraints is not None else []+        self.rbe2_elements = rbe2_elements if rbe2_elements is not None else []          # --- mesh data         conn, nid_to_idx, sorted_nids, elem_ids = mesh.connectivity_array()         self.conn = conn+        self.nid_to_idx = nid_to_idx         self.elem_ids = elem_ids         self.sorted_nids = np.array(sorted_nids)         self.n_nodes = len(sorted_nids)@@ -548,7 +567,9 @@ class DynamicSolver:         self.n_lambdas = sum(c.n_multipliers() for c in self.constraints)         self.n_total = self.n_dofs + self.n_extra + self.n_lambdas -        self.elem_coords = mesh.nodes_array()[conn]+        all_coords = mesh.nodes_array()+        self.elem_coords = all_coords[conn]+        self.coords = all_coords          # --- section (out-of-plane) thickness per element.         # section_thickness may be a scalar (uniform) or a {pid: t} dict so each@@ -575,6 +596,13 @@ class DynamicSolver:                  self.lam = np.zeros(self.n_lambdas, dtype=np.float64) +        # 罐 penalty drive: replaces BC-based 罐 prescription with a stiff+        # penalty spring on the extra-primal diagonal.  k_罐 >> structural+        # stiffness keeps tracking error negligible while preserving KKT+        # symmetry (vs BC row-zeroing which breaks PARDISO mtype=-2).+        self.theta_penalty_k: float = 0.0+        self.theta_targets: dict[int, float] = {}+         # --- precompute dof indices for assembly         self.dof_indices = np.zeros((self.n_elem, 8), dtype=np.int32)         self.dof_indices[:, 0::2] = self.conn * 2@@ -860,7 +888,27 @@ class DynamicSolver:             merit function for saddle-point systems).         """         f_int, _, _ = self._assemble(u_k, dt)-        ++        if self.rbe2_elements:+            for rbe2_elem in self.rbe2_elements:+                nids = [rbe2_elem.master_id] + rbe2_elem.slave_ids+                local_indices = [self.nid_to_idx[nid] for nid in nids]+                n_nodes = len(nids)++                u_elem = np.zeros(2 * n_nodes)+                for j, idx in enumerate(local_indices):+                    u_elem[2*j]     = u_k[2*idx]+                    u_elem[2*j + 1] = u_k[2*idx + 1]++                coords_elem = self.coords[local_indices]+                f_e, _, _ = rbe2_elem.compute_contributions(+                    coords_elem, u_elem, rbe2_elem.state+                )++                for j, idx in enumerate(local_indices):+                    f_int[2*idx]     += f_e[2*j]+                    f_int[2*idx + 1] += f_e[2*j + 1]+         a_k = (u_k - u_n - dt * v_n) * inv_beta_dt2 - (1.0 - 2.0 * beta) / (2.0 * beta) * a_n         a_ext_k = (u_ext_k - u_ext_n - dt * v_ext_n) * inv_beta_dt2 - (1.0 - 2.0 * beta) / (2.0 * beta) * a_ext_n @@ -894,8 +942,19 @@ class DynamicSolver:                                  lam_offset += n_lam             +        # 罐-penalty spring (must mirror solve_step residual assembly)+        if self.theta_penalty_k > 0.0:+            for off, target in self.theta_targets.items():+                R_ext[off] -= self.theta_penalty_k * (u_ext_k[off] - target)++        # Abaqus-style viscous stabilization: f_stab = c_stab/dt * (u_k - u_n)+        # _c_stab_per_dt is set in solve_step before the line-search calls this.+        _c_per_dt = getattr(self, '_c_stab_per_dt', 0.0)+        if _c_per_dt > 0.0:+            R_u = R_u - _c_per_dt * (u_k - u_n)+         R_total = np.concatenate([R_u, R_ext, R_lam])-        +         if len(self.bc_dofs) > 0:             for i, idx in enumerate(self.bc_dofs):                 val = self.bc_vals[i]@@ -904,7 +963,7 @@ class DynamicSolver:                 else:                     ext_idx = idx - self.n_dofs                     R_total[idx] = val - u_ext_k[ext_idx]-                +         return R_total      def solve_step(self, dt: float) -> int:@@ -988,8 +1047,15 @@ class DynamicSolver:         v_ext_n = self.v_extra.copy()         a_ext_n = self.a_extra.copy() -        u_k = u_n + dt * v_n + 0.5 * dt2 * a_n-        u_ext_k = u_ext_n + dt * v_ext_n + 0.5 * dt2 * a_ext_n+        # Quasistatic predictor: do NOT extrapolate with pseudo-velocity/acceleration+        # (they have no physical meaning in static mode).  u_k = u_n avoids+        # overshoot at the limit point and keeps Newton focused on the load increment.+        if self.static_mode:+            u_k = u_n.copy()+            u_ext_k = u_ext_n.copy()+        else:+            u_k = u_n + dt * v_n + 0.5 * dt2 * a_n+            u_ext_k = u_ext_n + dt * v_ext_n + 0.5 * dt2 * a_ext_n         lam_k = self.lam.copy()          # HHT-慣: compute f_int_n on first call (initial converged internal force)@@ -1018,13 +1084,50 @@ class DynamicSolver:          res_norm_0 = None         converged = False+        rbe2_trial_states = []+        _consecutive_stalls = 0   # stall detection: count consecutive line-search failures+        _STALL_LIMIT = 3          # fail early after this many consecutive stalls         for n_iter in range(self.max_iter):             # --- debug check             if np.any(np.isnan(u_k)):                 print(f"DEBUG: u_k has NaN before assembly at iter {n_iter+1}!", flush=True)                              f_int, K_T, state_new = self._assemble(u_k, dt)-            ++            # --- RBE2 element assembly ---+            if self.rbe2_elements:+                K_T = K_T.tolil()+                for rbe2_elem in self.rbe2_elements:+                    nids = [rbe2_elem.master_id] + rbe2_elem.slave_ids+                    local_indices = [self.nid_to_idx[nid] for nid in nids]+                    n_nodes = len(nids)++                    u_elem = np.zeros(2 * n_nodes)+                    for j, idx in enumerate(local_indices):+                        u_elem[2*j]     = u_k[2*idx]+                        u_elem[2*j + 1] = u_k[2*idx + 1]++                    coords_elem = self.coords[local_indices]+                    elem_state = rbe2_elem.state++                    f_e, K_e, state_new_elem = rbe2_elem.compute_contributions(+                        coords_elem, u_elem, elem_state+                    )++                    for j, idx in enumerate(local_indices):+                        f_int[2*idx]     += f_e[2*j]+                        f_int[2*idx + 1] += f_e[2*j + 1]++                    for a_idx, a_global in enumerate(local_indices):+                        for i in range(2):+                            for b_idx, b_global in enumerate(local_indices):+                                for j in range(2):+                                    K_T[2*a_global + i, 2*b_global + j] += K_e[2*a_idx + i, 2*b_idx + j]++                    rbe2_trial_states.append(state_new_elem)++                K_T = K_T.tocsr()+             # --- debug check             if np.any(np.isnan(f_int)) or (K_T is not None and np.any(np.isnan(K_T.data))):                 print(f"DEBUG: NaN detected in assembly at iter {n_iter+1}!", flush=True)@@ -1043,8 +1146,12 @@ class DynamicSolver:                 max_u_dof = 'UX' if max_u_idx % 2 == 0 else 'UY'                 print(f"  Max displacement magnitude in u_k: {u_k[max_u_idx]:.5e} at Node {max_u_node}({max_u_dof})", flush=True) -            a_k = (u_k - u_n - dt * v_n) * inv_beta_dt2 - (1.0 - 2.0 * beta) / (2.0 * beta) * a_n-            a_ext_k = (u_ext_k - u_ext_n - dt * v_ext_n) * inv_beta_dt2 - (1.0 - 2.0 * beta) / (2.0 * beta) * a_ext_n+            if self.static_mode:+                a_k = np.zeros_like(a_n)+                a_ext_k = np.zeros_like(a_ext_n)+            else:+                a_k = (u_k - u_n - dt * v_n) * inv_beta_dt2 - (1.0 - 2.0 * beta) / (2.0 * beta) * a_n+                a_ext_k = (u_ext_k - u_ext_n - dt * v_ext_n) * inv_beta_dt2 - (1.0 - 2.0 * beta) / (2.0 * beta) * a_ext_n              if self.static_mode:                 R_u = self.f_ext - f_int@@ -1052,6 +1159,7 @@ class DynamicSolver:                 R_u = self.f_ext - self.M * a_k - (1.0 + self.alpha) * f_int + self.alpha * self.f_int_n             else:                 R_u = self.f_ext - self.M * a_k - f_int+            R_u_mech = R_u.copy()             R_ext = np.zeros(self.n_extra)             R_lam = np.zeros(self.n_lambdas)             @@ -1085,7 +1193,14 @@ class DynamicSolver:                         R_lam[lam_offset + i] = -g[i]                                          lam_offset += n_lam-            ++            # 罐 penalty residual: drive 罐 DOFs toward targets via stiff+            # spring.  Must happen before R_total concatenation.+            if self.theta_penalty_k > 0.0:+                for off, target in self.theta_targets.items():+                    current = u_ext_k[off]+                    R_ext[off] -= self.theta_penalty_k * (current - target)+             # Combine Residual             R_total = np.concatenate([R_u, R_ext, R_lam]) @@ -1143,25 +1258,43 @@ class DynamicSolver:                     )                     k_extra_geo[off] += kval                 lam_off += n_lam_c+            # Adaptive extra-primal diagonal regularization.  The floor is+            # scaled with mean structural stiffness so it protects against+            # zero-diagonal pivots at 罐=0 (where k_geo=0) without polluting+            # the tangent once 罐?? and k_geo kicks in.+            _diag_eff = np.abs(K_eff.diagonal()).mean()+            _theta_reg = max(1e-8, 1e-8 * _diag_eff) if _diag_eff > 0 else 1e-8             for i in range(self.n_extra):+                diag_val = k_extra_geo[i] + _theta_reg+                # 罐 penalty spring: stiff spring toward target keeps tracking+                # error negligible without breaking KKT symmetry (cf BC-based+                # 罐 prescription which zeroes rows asymmetrically).+                if self.theta_penalty_k > 0.0 and i in self.theta_targets:+                    diag_val += self.theta_penalty_k                 global_row.append(ext_start_idx + i)                 global_col.append(ext_start_idx + i)-                global_val.append(k_extra_geo[i] + 1e-12)-                -            # Lambda stabilization (optional, avoid saddle point issues, usually spsolve handles it)-            # but sometimes zeros on diagonal cause UMFPACK to complain.-            # We will rely on spsolve unless it fails.-            +                global_val.append(diag_val)+             J = sps.coo_matrix((global_val, (global_row, global_col)), shape=(self.n_total, self.n_total)).tocsr() -            # --- Eigenvalue regularization (Tikhonov / ridge) ----            # Adds 琯쨌I to shift near-zero eigenvalues of the tangent stiffness-            # toward positive, improving conditioning without altering the-            # Newton direction for well-conditioned modes.  琯 is chosen small-            # enough to preserve accuracy (~machine-琯 횞 mean|diag(K)|).-            _diag_mean = np.abs(J.diagonal()[:self.n_dofs]).mean()-            _eps_reg = max(1e-12, 1e-8 * _diag_mean)-            J = J + _eps_reg * sps.eye(self.n_total, format='csr')+            # --- Abaqus-style viscous stabilization ---+            # Adds c_stab/dt to the mechanical diagonal and c_stab/dt*(u_k-u_n)+            # to the residual.  This shifts near-zero eigenvalues of K_T by a+            # physically motivated amount (proportional to the structural stiffness+            # scale) without corrupting the Newton direction for well-conditioned+            # modes.  Unlike 琯쨌I Tikhonov (which also shifts the LM/constraint+            # zero-diagonal rows), this acts only on mechanical DOFs.+            # Reference: Abaqus Analysis User's Guide 짠6.1.1 "Automatic stabilization"+            if self.viscous_stab_factor > 0.0:+                _k_mean = np.abs(J.diagonal()[:self.n_dofs]).mean() + 1e-30+                _c_stab_per_dt = self.viscous_stab_factor * _k_mean+                self._c_stab_per_dt = _c_stab_per_dt  # cache for _compute_R_total+                stab_diag = np.zeros(self.n_total)+                stab_diag[:self.n_dofs] = _c_stab_per_dt+                J = J + sps.diags(stab_diag, format='csr')+                R_total[:self.n_dofs] -= _c_stab_per_dt * (u_k - u_n)+            else:+                self._c_stab_per_dt = 0.0              if has_bc:                 for i, idx in enumerate(self.bc_dofs):@@ -1195,35 +1328,19 @@ class DynamicSolver:             du = du_all[:self.n_dofs]             du_ext = du_all[self.n_dofs:self.n_dofs+self.n_extra]             dlam = du_all[self.n_dofs+self.n_extra:]+            max_du_val = float(np.max(np.abs(du))) if self.n_dofs > 0 else 0.0 -            # --- Backtracking Line Search (residual-aware + under-relaxation) ----            # Full Newton steps (慣=1.0) are always accepted first, preserving-            # quadratic convergence in the attraction basin.  If the full step-            # causes the residual to EXPLODE (>10횞 R_current), the step is-            # likely pointing into a bifurcation branch that diverges ??the-            # tangent stiffness has lost positive-definiteness.  Subsequent-            # damped steps (慣=0.5, 0.25, ?? let the solver creep across the-            # unstable zone without blowing up.  Once past it, 慣=1.0 resumes.-            #-            # NOTE: a previous unconditional under-relaxation cap (alpha_max=0.7)-            # forced EVERY accepted step ??including the full Newton step ??down-            # to 0.7, which destroys quadratic convergence: with a consistent-            # tangent the residual then contracts only ~linearly and trivial-            # near-zero increments stall to max_iter (observed as constant du and-            # LS.alpha pinned at 0.70). Bifurcation regularization is already-            # provided by the LM diagonal shift, so the cap is both redundant and-            # harmful. The full Newton step (alpha=1.0) is now allowed; the loop-            # below only backtracks to escape NaN/Inf (geometric blow-up).+            # --- Backtracking Line Search ---             alpha = 1.0             alpha_min = 0.1-            alpha_max = 1.0        # no under-relaxation cap (full Newton allowed)             R_norm_current = np.linalg.norm(R_total)+            R_norm_temp = np.inf+            _ls_found_decrease = False -            while alpha > alpha_min + 1e-5:+            if R_norm_current < self.atol:                 u_temp = u_k + alpha * du                 u_ext_temp = u_ext_k + alpha * du_ext                 lam_temp = lam_k + alpha * dlam-                 R_temp = self._compute_R_total(                     u_temp, u_ext_temp, lam_temp,                     u_n, v_n, a_n,@@ -1231,21 +1348,61 @@ class DynamicSolver:                     dt, inv_beta_dt2, self.beta                 )                 R_norm_temp = np.linalg.norm(R_temp)-                 if np.isnan(R_norm_temp) or np.isinf(R_norm_temp):+                    alpha = 0.5+                else:+                    _ls_found_decrease = True+            else:+                while alpha > alpha_min + 1e-5:+                    u_temp = u_k + alpha * du+                    u_ext_temp = u_ext_k + alpha * du_ext+                    lam_temp = lam_k + alpha * dlam++                    R_temp = self._compute_R_total(+                        u_temp, u_ext_temp, lam_temp,+                        u_n, v_n, a_n,+                        u_ext_n, v_ext_n, a_ext_n,+                        dt, inv_beta_dt2, self.beta+                    )+                    # Apply BC modifications so R_norm_temp is comparable to+                    # R_norm_current (which was computed after BC row-zeroing).+                    if has_bc:+                        for i, idx in enumerate(self.bc_dofs):+                            val = self.bc_vals[i]+                            if idx < self.n_dofs:+                                R_temp[idx] = val - u_temp[idx]+                            else:+                                R_temp[idx] = val - u_ext_temp[idx - self.n_dofs]+                    R_norm_temp = np.linalg.norm(R_temp)++                    if np.isnan(R_norm_temp) or np.isinf(R_norm_temp):+                        alpha *= 0.5+                        continue++                    if R_norm_temp < R_norm_current:+                        _ls_found_decrease = True+                        break                     alpha *= 0.5-                    continue -                # Accept this 慣 if:-                #   (a) it is the full Newton step (subject to alpha_max cap), OR-                #   (b) residual has dropped / not exploded, OR-                #   (c) we are already deep in backtracking-                if alpha == 1.0 or R_norm_temp < R_norm_current * 10.0 or alpha < 0.25:-                    break-                alpha *= 0.5--            # Enforce under-relaxation cap-            alpha = min(alpha, alpha_max)+                # Re-evaluate at the final alpha so the post-step residual is correct.+                if not _ls_found_decrease:+                    u_temp = u_k + alpha * du+                    u_ext_temp = u_ext_k + alpha * du_ext+                    lam_temp = lam_k + alpha * dlam+                    R_temp = self._compute_R_total(+                        u_temp, u_ext_temp, lam_temp,+                        u_n, v_n, a_n,+                        u_ext_n, v_ext_n, a_ext_n,+                        dt, inv_beta_dt2, self.beta+                    )+                    if has_bc:+                        for i, idx in enumerate(self.bc_dofs):+                            val = self.bc_vals[i]+                            if idx < self.n_dofs:+                                R_temp[idx] = val - u_temp[idx]+                            else:+                                R_temp[idx] = val - u_ext_temp[idx - self.n_dofs]+                    R_norm_temp = np.linalg.norm(R_temp)              # If line search failed to find a non-NaN/non-inf residual             if np.isnan(R_norm_temp) or np.isinf(R_norm_temp):@@ -1255,12 +1412,32 @@ class DynamicSolver:                     print(f"  *** ERROR: Line search failed to resolve NaN/Inf residual at iteration {n_iter+1}. (Time: {t_elapsed:.2f} s)\n", flush=True)                 return -(n_iter + 1) +            # --- Stall detection ---+            # When the line search consistently fails to find a decreasing+            # direction (alpha ends at alpha_min every time), the Newton+            # direction is systematically wrong ??no amount of extra+            # iterations will fix it.  Fail early to avoid wasting time.+            if not _ls_found_decrease:+                _consecutive_stalls += 1+                if _consecutive_stalls >= _STALL_LIMIT:+                    if getattr(self, 'verbose', False):+                        t_elapsed = time.time() - t_start+                        print(f"  ---------------------------------------------------------------------------------------------------------------------", flush=True)+                        print(f"  *** STALL: Line search failed {_consecutive_stalls}x in a row (alpha={alpha:.4f}, res={R_norm_temp:.4e}). Newton direction is wrong. Aborting early. (Time: {t_elapsed:.2f} s)\n", flush=True)+                    return -(n_iter + 1)+            else:+                _consecutive_stalls = 0   # good step resets counter+             du *= alpha             du_ext *= alpha             dlam *= alpha             du_all *= alpha-            -            res_norm = R_norm_current++            # FIX: Use the POST-step residual (R_norm_temp) for convergence+            # checks, not the pre-step R_norm_current.  The old code used+            # R_norm_current which meant the convergence criteria never saw+            # the actual residual after the step was applied.+            res_norm = R_norm_temp             if res_norm_0 is None:                 res_norm_0 = res_norm + 1e-16             res_ratio = res_norm / res_norm_0@@ -1275,14 +1452,13 @@ class DynamicSolver:                              u_norm = np.linalg.norm(u_k)             rel_change = du_norm / (u_norm + 1e-12)++            active_dofs = np.ones(self.n_dofs, dtype=bool)+            if len(self.bc_dofs) > 0:+                node_bc_dofs = self.bc_dofs[self.bc_dofs < self.n_dofs]+                active_dofs[node_bc_dofs] = False                          if getattr(self, 'verbose', False):-                # 1. Max active force residual-                active_dofs = np.ones(self.n_dofs, dtype=bool)-                if len(self.bc_dofs) > 0:-                    node_bc_dofs = self.bc_dofs[self.bc_dofs < self.n_dofs]-                    active_dofs[node_bc_dofs] = False-                                 if np.any(active_dofs):                     abs_R_active = np.abs(R_u) * active_dofs                     max_R_val = np.max(abs_R_active)@@ -1351,22 +1527,47 @@ class DynamicSolver:             # floors at the linear-solver precision ~1e-6 absolute) and never             # fired, so steps with a near-zero displacement ratio stalled to             # max_iter even when already converged.-            residual_converged = (-                res_norm_0 is not None and res_ratio < self.rtol+            # ---- True equilibrium residual (after constraints + stab) ----+            # Use R_temp from the accepted line-search state (stabilized, post-step).+            # R_u is stale (pre-line-search, unstabilized) and gives a false+            # residual reading at the limit point.+            _R_post = R_temp[:self.n_dofs] if 'R_temp' in dir() else R_u+            max_R_equilibrium = 0.0+            if np.any(active_dofs):+                max_R_equilibrium = float(np.max(np.abs(_R_post[active_dofs])))++            # ---- Convergence criteria (commercial-style: require at least one+            # displacement criterion AND at least one force criterion) ----+            disp_criterion = (+                rel_change < self.tol+                or max_du_val < self.max_du_atol+                or du_norm < self.atol             )-            # Absolute convergence (P2): when the Newton correction itself is-            # negligibly small, the increment has converged regardless of the-            # *relative* disp ratio. This cures the near-zero-deformation stall-            # of a flat (C2) amplitude start and saddle-point noise, where-            # du_norm/u_norm is dominated by numerical noise (0/0) and never-            # reaches `tol` even though |du| ~ 1e-11.-            abs_converged = du_norm < self.atol-            if (rel_change < self.tol or energy_err < 1e-15-                    or residual_converged or abs_converged) and n_iter > 0:+            _residual_small_enough_for_energy_criterion = max_R_equilibrium < max(1e6, 10.0 * self.force_atol)+            force_criterion = (+                (res_norm_0 is not None and res_ratio < self.rtol)+                or max_R_equilibrium < self.force_atol+                or (energy_err < max(1e-15, res_norm * 1e-6) and _residual_small_enough_for_energy_criterion)+            )+            # Physical divergence guard: reject convergence if max|u| exceeds+            # a user-set limit (e.g., panel half-width 횞 safety factor).+            # Large u indicates a non-physical state (element inversion /+            # Lagrange multiplier blow-up) masked by small displacement ratio.+            _max_u = np.max(np.abs(u_k))+            _disp_limit = getattr(self, 'max_disp_limit', None)+            if _disp_limit is not None and _max_u > _disp_limit:+                if getattr(self, 'verbose', False):+                    print(f"  *** DIVERGE: max|u|={_max_u:.1f} > limit {_disp_limit:.1f}. Failing step.", flush=True)+                return -(n_iter + 1)++            if disp_criterion and force_criterion and n_iter > 0:                 converged = True                 self.time += dt          # advance analysis time on success only                 if state_new is not None:                     self.state = state_new+                if self.rbe2_elements and rbe2_trial_states:+                    for i, rbe2_elem in enumerate(self.rbe2_elements):+                        rbe2_elem.state = rbe2_trial_states[i]                 if self.alpha != 0.0:                     self.f_int_n = f_int.copy()  # store for HHT-慣 next step                 if getattr(self, 'verbose', False):@@ -1380,13 +1581,20 @@ class DynamicSolver:             lam_k += dlam          self.u = u_k.copy()-        # Guard against unphysical acceleration / velocity spikes due to tiny dt cutbacks-        self.a = np.clip(a_k.copy(), -1.0e4, 1.0e4)-        self.v = np.clip(v_n + dt * ((1.0 - gamma) * a_n + gamma * a_k), -1.0e3, 1.0e3)-                 self.u_extra = u_ext_k.copy()-        self.a_extra = np.clip(a_ext_k.copy(), -1.0e4, 1.0e4)-        self.v_extra = np.clip(v_ext_n + dt * ((1.0 - gamma) * a_ext_n + gamma * a_ext_k), -1.0e3, 1.0e3)+        if self.static_mode:+            # Quasistatic: velocity/acceleration are not physical quantities.+            # Zero them so the next predictor starts from a clean state.+            self.a = np.zeros_like(self.a)+            self.v = np.zeros_like(self.v)+            self.a_extra = np.zeros_like(a_ext_n)+            self.v_extra = np.zeros_like(v_ext_n)+        else:+            # Guard against unphysical acceleration / velocity spikes due to tiny dt cutbacks+            self.a = np.clip(a_k.copy(), -1.0e4, 1.0e4)+            self.v = np.clip(v_n + dt * ((1.0 - gamma) * a_n + gamma * a_k), -1.0e3, 1.0e3)+            self.a_extra = np.clip(a_ext_k.copy(), -1.0e4, 1.0e4)+            self.v_extra = np.clip(v_ext_n + dt * ((1.0 - gamma) * a_ext_n + gamma * a_ext_k), -1.0e3, 1.0e3)                  self.lam = lam_k.copy() @@ -2032,7 +2240,7 @@ class DynamicSolver:                             good_K = K_es_np[good_mask] * np.asarray(t_b[good_mask])[:, None, None]                             good_dofs = self.dof_indices[elem_indices[good_mask]]                             np.add.at(f_int, good_dofs.flatten(), good_f.flatten())-                            all_K_rows.append(good_dofs.reshape(-1))+                            all_K_rows.append(np.repeat(good_dofs, 8, axis=1).reshape(-1))                             all_K_cols.append(np.repeat(good_dofs, 8, axis=1).reshape(-1))                             all_K_vals.append(good_K.reshape(-1))                         from ..element.q4_eas import compute_eas_j2_contributions@@ -2042,11 +2250,25 @@ class DynamicSolver:                             u_elem = u[self.dof_indices[e]]                             state_elem = (self.state[e, :, :n_vars]                                           if self.state is not None else None)-                            f_e, K_e, alpha_new, se_new = compute_eas_j2_contributions(-                                coords, u_elem, self.eas_alpha[e], state_elem,-                                mat_adapter.material, mat_adapter.params,-                                self._elem_thickness[e],-                            )+                            try:+                                f_e, K_e, alpha_new, se_new = compute_eas_j2_contributions(+                                    coords, u_elem, self.eas_alpha[e], state_elem,+                                    mat_adapter.material, mat_adapter.params,+                                    self._elem_thickness[e],+                                )+                            except (ZeroDivisionError, ValueError,+                                    FloatingPointError) as exc:+                                print(f"  WARNING: Element {e} extreme deformation ({exc}). "+                                      "Injecting NaN to force step cutback.")+                                # NaN propagates through assembly ??residual NaN ??line search+                                # backtracks to alpha_min ??step fails ??caller cuts dt.+                                # Do NOT zero: zeroing masks the inversion and allows+                                # false convergence (displacement ratio small despite huge residual).+                                f_e = np.full(8, np.nan, dtype=np.float64)+                                K_e = np.zeros((8, 8), dtype=np.float64)+                                alpha_new = self.eas_alpha[e]+                                se_new = (self.state[e, :, :n_vars].copy()+                                          if state_new is not None else None)                             self.eas_alpha[e] = alpha_new                             np.add.at(f_int, self.dof_indices[e], f_e)                             if state_new is not None and se_new is not None:@@ -2397,10 +2619,16 @@ class DynamicSolver:             state_new = None          elif self.use_j2_batch:-            return self._assemble_j2_batch(u, dt)+            # NOTE: assign (do NOT early-return) so execution falls through to the+            # shared penalty-constraint block below. A previous early `return`+            # here silently skipped ALL penalty constraints (contact + spring+            # drive) for the batch assembly paths ??the penalty forces never+            # entered f_int/K_T, so e.g. a penalty hinge drive produced zero+            # residual and the structure never moved (false convergence at u=0).+            f_int, K_T, state_new = self._assemble_j2_batch(u, dt)          elif self.use_multi_material_batch:-            return self._assemble_multi_material_batch(u, dt)+            f_int, K_T, state_new = self._assemble_multi_material_batch(u, dt)          elif self.use_jax_grouped_vmap:             # --- Multi-material Grouped Vectorized JAX Assembly ---